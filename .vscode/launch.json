{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "cppvsdbg",
            "request": "launch",
            "name": "Launch",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "Build",
            "program": "${workspaceRoot}/prog.exe"
        }
    ]
}